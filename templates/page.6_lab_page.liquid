{% render 'shogun-products', content: page %}
<h1>{{ page.title }}</h1>
<div>{{ page.content }}</div>
<main id ="6lab-page">
    <div id="test-lab">
<div class="text-lab-link">  
  <div class="lab-contain"><a href="#soil" class="scroll"><input type="radio" name="lab" value="soil" /></a> <label for="soil">soil lab</label></div>
  <div class="lab-contain"><a href="#extractionn" class="scroll"><input type="radio"  name="lab" value="extration" /> </a><label for="extration">extration lab</label></div>
  <div class="lab-contain"><a href="#molecular" class="scroll"><input type="radio" name="lab" value="molecular" /></a> <label for="molecular">molecular lab</label></div>
  <div class="lab-contain"><a href="#formulationn" class="scroll"><input type="radio" name="lab" value="formulation" /></a> <label for="formulation">formulation lab</label></div>
  <div class="lab-contain"><a href="#design" class="scroll"><input type="radio" name="lab" value="design" /></a> <label for="design">design lab</label></div>
  <div class="lab-contain"><a href="#recycle" class="scroll"><input type="radio" name="lab" value="recycle" /></a> <label for="recycle">recycle lab</label></div>

</div>
</div>
{%section '6-lab-agriculture'%}
  {%section '6lab-agriculture-2'%} 
  {%section '6-lab-hero'%}  
  {%section '6-lab-Image-text'%} 
  {%section '6lab-agriculture-3'%}
  {%section '6-lab-hero-2'%}
  {%section '6-lab-image-text-2'%}
  {%section '6-lab-agriculture-4'%}
  {%section '6-lab-hero-3'%}
  {%section '6-lab-image-text-3'%}
</main>
<script>
$(document).ready(function() {
  var topOfOthDiv = 100;
  $(window).scroll(function() {
    if($(window).scrollTop() > topOfOthDiv) {
       $("#test-lab").addClass('show');
    } else {
       $("#test-lab").removeClass('show');
    }
  });
});
  </script>
<script>
    var lastId,
    topMenu = $("#test-lab"),
    topMenuHeight = topMenu.outerHeight()+15,
    menuItems = topMenu.find("a"),
    scrollItems = menuItems.map(function(){
      var item = $($(this).attr("href"));
      if (item.length) { return item; }
    });
menuItems.click(function(e){
  var href = $(this).attr("href"),
      offsetTop = href === "#" ? 0 : $(href).offset().top-topMenuHeight+1;
  $('html, body').stop().animate({ 
      scrollTop: offsetTop
  }, 300);
  e.preventDefault();
});

$(window).scroll(function(){
   var fromTop = $(this).scrollTop()+topMenuHeight;
    var cur = scrollItems.map(function(){
     if ($(this).offset().top < fromTop)
       return this;
   });
   cur = cur[cur.length-1];
   var id = cur && cur.length ? cur[0].id : "";
   if (lastId !== id) {
       lastId = id;
       menuItems
         .parent().removeClass("cllicked")
         .end().filter("[href='#"+id+"']").parent().addClass("cllicked");
   }                   
});
</script>